'use strict';

describe('mixtura.search module', function() {

  beforeEach(module('mixtura.search'));

  describe('search controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('searchController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});